// const order = require('../sequelize').order
module.exports = (app) => {

    // app.get('/purchasingOrder', (req, res) => {
    //     console.log(req);
    //     order.findAll().then(foundOrders=>{
    //         foundOrders = JSON.stringify(foundOrders)
    //         res.send(foundOrders)
    //     })
    // })
}